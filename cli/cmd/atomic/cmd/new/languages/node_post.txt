const drift = require('@ondrift/sdk'); // secrets, nosql, blobs, queues, cache, locks, jwt, httpRequest, log

{{ANNOTATION}}
function {{FUNC}}(body, req) {
    if (!body.name) {
        return [400, 'Bad Request', { error: 'name is required' }];
    }
    return [201, 'Created', { hello: body.name }];
}

module.exports = { {{FUNC}} };
