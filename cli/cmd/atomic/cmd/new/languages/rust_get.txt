use drift_sdk::Value; // drift_sdk::{secret, cache, nosql, queue, blob, lock, jwt, log, http_request}
use serde_json::json;

{{ANNOTATION}}
pub fn {{FUNC}}(req: Value) -> (i64, &'static str, Value) {
    let _ = req;
    (200, "OK", json!({ "hello": "from drift" }))
}
